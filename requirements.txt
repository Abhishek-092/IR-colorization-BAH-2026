# Project SUTRAM — runtime requirements (Python 3.9–3.12)
# Torch auto-selects CUDA / Apple-MPS / CPU at runtime; no GPU required.
torch>=2.2.0
torchvision>=0.17.0
numpy>=1.24.0
flask>=3.0.0
Pillow>=10.0.0
tifffile>=2023.7.10
imagecodecs>=2023.3.16
rasterio>=1.3.0
omegaconf>=2.3.0
hydra-core>=1.3.2
scipy>=1.10.0
opencv-python-headless>=4.8.0
matplotlib>=3.7.0
requests>=2.31.0
# Track-2 photorealistic refiner (SD-Turbo) — ON by default. First run
# downloads ~2.5 GB of SD-Turbo weights (needs internet, one time). If these
# are absent the dashboard still runs; the refiner simply no-ops.
diffusers>=0.27.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0
# optional (evaluation / export / tests)
onnx>=1.14.0
onnxruntime>=1.15.0
pytest>=7.0.0
